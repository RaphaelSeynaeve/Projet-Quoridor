#include "define et fonctions.h"


void nouvellePartie(char plateau[TAILLE_PLATEAU][TAILLE_PLATEAU], Joueur joueurs[2]) {
    initialiserPlateau(plateau);  // Initialise le plateau
    initialiserJoueurs(joueurs); // Initialise les joueurs
}
